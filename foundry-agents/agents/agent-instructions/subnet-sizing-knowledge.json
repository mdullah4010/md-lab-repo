{
  "subnet_sizing_rules": [
    {
      "min_servers": 0,
      "max_servers": 3,
      "subnet_mask": "/29",
      "description": "For 1-3 servers in a layer, use a /29 subnet."
    },
    {
      "min_servers": 4,
      "max_servers": 10,
      "subnet_mask": "/28",
      "description": "For 4-10 servers in a layer, use a /28 subnet."
    },
    {
      "min_servers": 11,
      "max_servers": 30,
      "subnet_mask": "/27",
      "description": "For 11-30 servers in a layer, use a /27 subnet."
    }
  ],
  "app_gateway_note": "If Azure Application Gateway is present, allocate a dedicated subnet for it regardless of server count."
}